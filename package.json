{
  "scripts": {
    "rebuild:full": "rimraf node_modules && npm install && npm run rebuild",
    "rebuild": "lerna clean --yes && lerna bootstrap && lerna run test"
  },
  "dependencies": {
    "lerna": "2.0.0-rc.4",
    "rimraf": "^2.6.1"
  },
  "devDependencies": {
    "react": "^15.6.1",
    "react-dom": "^15.6.1"
  }
}
